/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package ac.za.cput.controlclerkform;
//import com.mysql.jdbc.jdbc2.optional.MysqlDataSource;

/**
 *
 * @author A. Mjeks
 */
/*public class MyConnection {
    private static String servername = "localhost";//localhost
    private static String username = "mjekula";//root
    private static String dbname = "inventory-system";
    private static Integer portnumber = 3308;//3306
    private static String password = "Athenkosi52044";//""

    public static Connection getConnection() {
        Connection connect = null;

        MysqlDataSource datasource = new MysqlDataSource();
        datasource.setServerName(servername);
        datasource.setUser(username);
        datasource.setPassword(password);
        datasource.setDatabaseName(dbname);
        datasource.setPortNumber(portnumber);

        try {
            connect = (Connection) datasource.getConnection();

        } catch (SQLException ex) {
            System.out.println("SQLException: " + ex.getMessage());
        }
        return connect;
    }
}*/
